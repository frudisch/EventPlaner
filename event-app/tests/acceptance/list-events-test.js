import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | list events', function(hooks) {
  setupApplicationTest(hooks);

/*
  test('should show a list of events as the home page', async function (assert) {
  });

  test('should filter the list of events by name.', async function (assert) {
  });

  test('should show details for a selected event', async function (assert) {
  });
*/
});
